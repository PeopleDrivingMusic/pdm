test-results
node_modules

# yarn.lock is the source of truth; an npm lockfile drifts and double-reports
# every Dependabot alert.
package-lock.json

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Database
# drizzle/migrations  # Keep migrations in git for team sync
docker/postgres/data/
*.sql.bak
*.dump

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

coverage/

# Claude Code (local config + AI-maintained wiki)
.claude/
.agents/
.mcp.json
skills-lock.json

# playwright-mcp session output (page snapshots), not project source
.playwright-mcp/

# Large demo/seed media (Metallica mp3s + placeholder art) — never belonged in
# git; ~130MB of it made this branch's first push to origin impractical.
# Untracked here going forward, kept on disk locally for dev/demo purposes.
# Regenerate/reacquire per-machine rather than cloning them.
pen/
static/music/
static/bg/
static/albums/
static/artists/cover/
static/uploads/tracks/
static/muse.webp
static/rock_am_ring.jpeg
