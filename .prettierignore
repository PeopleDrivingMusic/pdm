# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Supabase CLI's local link cache (already gitignored via supabase/.gitignore,
# but prettier doesn't read nested .gitignore files)
supabase/.temp/
